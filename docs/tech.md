# 技术文档

本文档详细介绍 ACM-Compass 项目的技术架构、目录结构、数据存储格式以及核心模块的实现细节。

---

## 目录结构

```
ACM-Compass/
├── server.py                  # 应用入口，启动 FastAPI + Gradio 服务
├── bookmarklet.js             # 浏览器书签脚本源码
├── generate_bookmarklet.py    # 生成 bookmarklet.html 的脚本
├── pyproject.toml             # 项目配置和依赖定义
├── requirements.txt           # pip 依赖列表
├── uv.lock                    # uv 锁定文件
├── .python-version            # Python 版本要求 (3.13+)
├── .git_config.json           # Git 同步配置缓存（运行时生成）
├── docs/
│   ├── tech.md                # 本技术文档
│   ├── README_EN.md           # 英文 README
│   ├── banner.png             # 项目 Banner 图片
│   └── banner.svg             # 项目 Banner SVG
├── src/
│   ├── __init__.py
│   ├── models.py              # Pydantic 数据模型定义
│   ├── data_manager.py        # 数据管理层（文件 I/O）
│   ├── git_utils.py           # Git 版本控制工具
│   └── ui/
│       ├── __init__.py
│       ├── banner.py          # 启动时的 ASCII Art Banner
│       ├── problems.py        # 题目管理 UI 模块
│       ├── contests.py        # 比赛管理 UI 模块
│       └── git_sync.py        # Git 同步 UI 模块
└── data/                      # 数据存储目录（独立 Git 仓库）
    ├── problems.json          # 题目数据
    ├── contests.json          # 比赛数据
    └── solutions/             # 题解 Markdown 文件目录
        └── {problem_id}.md    # 每道题的题解文件
```

---

## 技术栈

| 组件 | 技术 | 版本要求 |
|------|------|----------|
| 运行环境 | Python | >= 3.13 |
| Web 框架 | FastAPI | >= 0.121.1 |
| UI 框架 | Gradio | >= 5.49.1 |
| Web 服务器 | Uvicorn | >= 0.38.0 |
| 数据验证 | Pydantic | >= 2.11.10 |
| 数据处理 | Pandas | >= 2.2.3 |
| Markdown 扩展 | pymdown-extensions | >= 10.12 |

---

## 数据存储格式

### 数据文件位置

- **题目数据**: `data/problems.json`
- **比赛数据**: `data/contests.json`
- **题解文件**: `data/solutions/{problem_id}.md`
- **Git 配置缓存**: `.git_config.json`

### 题目数据结构 (problems.json)

```json
[
  {
    "id": "uuid-v4-string",
    "title": "题目标题",
    "link": "https://example.com/problem/123",
    "source": "Codeforces",
    "tags": ["dp", "graph", "数学"],
    "assignee": "补题人姓名",
    "solved": false,
    "unsolved_stage": "未看题",
    "unsolved_custom_label": "自定义标签",
    "pass_count": 35,
    "notes": "备注内容",
    "created_at": "2024-01-15T10:30:00+00:00",
    "updated_at": "2024-01-16T14:20:00+00:00"
  }
]
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | UUID v4 格式的唯一标识符（系统自动生成） |
| `title` | string | 是 | 题目标题 |
| `link` | string\|null | 否 | 题目链接（HTTP/HTTPS URL） |
| `source` | string\|null | 否 | 题目来源（如 Codeforces、AtCoder、Luogu 等） |
| `tags` | string[] | 否 | 标签列表 |
| `assignee` | string\|null | 否 | 当前负责补题的队员 |
| `solved` | boolean | 是 | 是否已解决 |
| `unsolved_stage` | string\|null | 否 | 未解决阶段分类（`"未看题"` \| `"已看题无思路"` \| `"知道做法未实现"`） |
| `unsolved_custom_label` | string\|null | 否 | 自定义未解决标签 |
| `pass_count` | integer\|null | 否 | 场上通过人数（≥0） |
| `notes` | string\|null | 否 | 备注信息 |
| `created_at` | string | 是 | 创建时间（ISO 8601 格式） |
| `updated_at` | string | 是 | 更新时间（ISO 8601 格式） |

> **注意**: `has_solution` 字段是运行时动态计算的，不存储在 JSON 中。它根据 `data/solutions/{id}.md` 文件是否存在来判断。

### 比赛数据结构 (contests.json)

```json
[
  {
    "id": "uuid-v4-string",
    "name": "比赛名称",
    "total_problems": 12,
    "problems": [
      {
        "letter": "A",
        "pass_count": 150,
        "attempt_count": 200,
        "my_status": "ac"
      },
      {
        "letter": "B",
        "pass_count": 80,
        "attempt_count": 120,
        "my_status": "attempted"
      }
    ],
    "rank_str": "25/300",
    "summary": "赛后总结内容...",
    "created_at": "2024-01-15T10:30:00+00:00",
    "updated_at": "2024-01-16T14:20:00+00:00"
  }
]
```

**字段说明**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | string | 是 | UUID v4 格式的唯一标识符（系统自动生成） |
| `name` | string | 是 | 比赛名称 |
| `total_problems` | integer | 是 | 题目数量（1-26） |
| `problems` | object[] | 是 | 题目统计数组 |
| `problems[].letter` | string | 是 | 题目字母（A-Z） |
| `problems[].pass_count` | integer | 是 | 通过人数 |
| `problems[].attempt_count` | integer | 是 | 尝试人数 |
| `problems[].my_status` | string | 是 | 本队状态（`"ac"` \| `"attempted"` \| `"unsubmitted"`） |
| `rank_str` | string\|null | 否 | 排名字符串（如 "25/300"） |
| `summary` | string\|null | 否 | 赛后总结 |
| `created_at` | string | 是 | 创建时间（ISO 8601 格式） |
| `updated_at` | string | 是 | 更新时间（ISO 8601 格式） |

### 题解文件 (solutions/*.md)

题解文件以题目 ID 命名，存储为 Markdown 格式：

- **文件路径**: `data/solutions/{problem_id}.md`
- **格式**: Markdown，支持 LaTeX 公式
- **LaTeX 语法**:
  - 行内公式: `$E=mc^2$`
  - 块级公式: `$$\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$$`

### Git 配置缓存 (.git_config.json)

```json
{
  "repo_url": "https://github.com/username/acm-data.git",
  "branch": "main",
  "cloned": true,
  "last_updated": "2024-01-15T10:30:00"
}
```

---

## 核心模块

### 1. 数据模型 (src/models.py)

使用 Pydantic v2 定义数据模型，提供类型验证和数据规范化：

```python
# 常量定义
UnsolvedStage = Literal["未看题", "已看题无思路", "知道做法未实现"]
ContestStatus = Literal["ac", "attempted", "unsubmitted"]
LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"

# 核心模型
class ProblemIn(BaseModel):    # 题目输入模型
class Problem(ProblemIn):       # 完整题目模型（含系统字段）
class ContestProblemIn(BaseModel): # 比赛题目输入模型
class ContestIn(BaseModel):     # 比赛输入模型
class Contest(ContestIn):       # 完整比赛模型（含系统字段）
```

**数据验证特性**:
- 字符串字段自动去除首尾空白
- 空字符串自动转换为 `None`
- `solved=True` 时自动清除未解决相关字段
- 通过人数验证为非负整数

### 2. 数据管理 (src/data_manager.py)

负责所有文件 I/O 操作，使用线程锁保证并发安全：

```python
# 路径常量
BASE_DIR = Path(__file__).parent.parent  # 项目根目录
DATA_DIR = BASE_DIR / "data"
DATA_FILE = DATA_DIR / "problems.json"
CONTESTS_FILE = DATA_DIR / "contests.json"
SOLUTIONS_DIR = DATA_DIR / "solutions"

# 核心函数
def load_problems() -> List[dict]       # 加载题目列表
def save_problems(items: List[dict])    # 保存题目列表
def create_problem(data: dict) -> dict  # 创建新题目
def update_problem(id, data) -> dict    # 更新题目
def delete_problem(id) -> bool          # 删除题目

def load_contests() -> List[dict]       # 加载比赛列表
def save_contests(items: List[dict])    # 保存比赛列表
def create_contest(data: dict) -> dict  # 创建新比赛
def update_contest(id, data) -> dict    # 更新比赛
def delete_contest(id) -> bool          # 删除比赛

def read_solution(id) -> Optional[str]  # 读取题解
def write_solution(id, markdown)        # 保存题解
def delete_solution(id)                 # 删除题解
def solution_exists(id) -> bool         # 检查题解是否存在
```

**向后兼容性**:
- `normalize_record()` 函数处理旧版数据格式迁移
- 自动将旧版 `solution` 字段迁移到独立的 Markdown 文件
- 自动处理损坏的 JSON 文件（创建备份后重置）

### 3. Git 工具 (src/git_utils.py)

提供 Git 版本控制功能，支持独立的 data 仓库：

```python
# 核心功能
def clone_data_repo(repo_url, branch) -> str  # 克隆远程仓库到 data/
def git_pull(repo_url, branch) -> str         # 拉取远程更新
def git_push(repo_url, message, branch) -> str # 推送本地更改
def backup_and_reclone(repo_url, branch) -> str # 备份并重新克隆

# 辅助功能
def is_data_git_repo() -> bool      # 检查 data/ 是否为独立 Git 仓库
def get_remote_url() -> Optional[str] # 获取远程仓库 URL
def get_current_branch() -> Optional[str] # 获取当前分支
def get_repo_status() -> str        # 获取仓库状态信息
def load_git_config() -> dict       # 加载 Git 配置缓存
def save_git_config(...)            # 保存 Git 配置缓存
```

**设计理念**:
- `data/` 目录作为独立的 Git 仓库，与项目代码仓库分离
- 支持私有仓库同步训练数据
- 自动备份现有数据，防止数据丢失

### 4. UI 模块 (src/ui/)

使用 Gradio 构建响应式 Web 界面：

| 模块 | 文件 | 功能 |
|------|------|------|
| 题目管理 | `problems.py` | 三个标签页：全部题目、未解决、已解决 |
| 比赛管理 | `contests.py` | 比赛记录、题目统计、浏览器导入 |
| Git 同步 | `git_sync.py` | 仓库配置、拉取/推送操作 |
| Banner | `banner.py` | 启动时的 ASCII Art 显示 |

**UI 特性**:
- 响应式表格展示
- 日期范围筛选
- 点击 ID 快速加载数据到表单
- Markdown + LaTeX 题解预览
- 动态显示/隐藏题目输入框

---

## 应用架构

### 服务器入口 (server.py)

```python
# FastAPI + Gradio 混合架构
fastapi_app = FastAPI()
fastapi_app.add_middleware(CORSMiddleware, ...)  # CORS 支持

# API 端点
@fastapi_app.post("/api/import_contest")
async def api_import_contest(request: Request):
    # 接收浏览器书签发送的比赛数据
    ...

# Gradio 应用
def build_app():
    with gr.Blocks(...) as app:
        with gr.Tabs():
            build_problem_tab("全部题目", "📚", "all")
            build_problem_tab("未解决", "⚠️", "unsolved")
            build_problem_tab("已解决", "✅", "solved")
            build_contest_tab()
            build_git_sync_tab()
    return app

# 挂载 Gradio 到 FastAPI
gr.mount_gradio_app(fastapi_app, app, path="/")
uvicorn.run(fastapi_app, host="127.0.0.1", port=7860)
```

### 浏览器书签工具 (bookmarklet.js)

用于从 qoj.ac/ucup.ac 的 standings 页面提取比赛数据：

1. **数据提取**:
   - 从 `<h1>` 获取比赛名称
   - 从 `<thead>` 获取题目列表和统计信息
   - 从 `.table-warning` 行获取用户排名和做题状态

2. **状态识别**:
   - `+` 或 `+N` → AC
   - `-N` → 尝试但未通过
   - 空单元格 → 未提交

3. **数据发送**:
   - POST 到 `http://127.0.0.1:7860/api/import_contest`
   - 使用 Gradio 格式: `{"data": [contestData]}`

---

## 开发指南

### 环境设置

```bash
# 使用 uv（推荐）
uv sync

# 或使用 pip
pip install -r requirements.txt
```

### 运行服务

```bash
# 方式一
python server.py

# 方式二（使用 uv）
uv run python server.py
```

### 生成书签工具页面

```bash
python generate_bookmarklet.py
# 生成 bookmarklet.html
```

### 添加新功能

1. **添加新字段到数据模型**:
   - 更新 `src/models.py` 中的 Pydantic 模型
   - 更新 `src/data_manager.py` 中的 `normalize_*` 函数以处理向后兼容

2. **添加新 UI 组件**:
   - 在 `src/ui/` 中创建新模块
   - 在 `server.py` 的 `build_app()` 中注册

3. **添加新 API 端点**:
   - 在 `server.py` 中使用 `@fastapi_app` 装饰器定义

---

## 数据同步工作流

```
┌─────────────────────────────────────────────────────────────┐
│                       用户操作                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Gradio UI (problems.py / contests.py)                       │
│  - 表单输入验证                                              │
│  - 事件处理                                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Data Manager (data_manager.py)                              │
│  - 数据规范化                                                │
│  - 文件 I/O（线程安全）                                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  本地文件系统                                                │
│  - data/problems.json                                        │
│  - data/contests.json                                        │
│  - data/solutions/*.md                                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Git 同步 (git_utils.py)                                     │
│  - git pull / push                                           │
│  - 远程仓库同步                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 安全注意事项

1. **本地服务**: 服务默认绑定 `127.0.0.1`，仅限本机访问
2. **CORS**: 允许所有来源（为了支持书签工具）
3. **Git 凭证**: 使用系统 Git 凭证管理，不在应用中存储密码
4. **数据备份**: Git 操作前自动备份现有数据

---

## 许可证

本项目采用 GPL-3.0 许可证。

